# Core pipeline (steps 1-3, 6-7, spot check). Python 3.10+.
anthropic>=1.4
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
pyarrow>=15
pyogrio>=0.9
rasterio>=1.3
pystac-client>=0.8
planetary-computer>=1.0
pillow>=10
numpy>=1.26
pandas>=2.1
requests>=2.31
pyyaml>=6
pytest>=8

# Detection (steps 4-5) is heavy (torch). Install separately:
#   pip install -r requirements-train.txt
